---
src_subdir: "{{ project_name }}"
instance_name: proto
debug: true
src_as_vol: true
domain: proto.kultio.com
wsgi_root: /app
wsgi_log_dir: /var/log/wsgi

smtp_server: auth.smtp.1and1.fr
smtp_port: 465
smtp_user: bot@kultio.com
smtp_use_tls: True
